#include <stdio.h>
#include <stdlib.h>

int main(){
    int a,b,num;
    scanf("%d %d %d", &a, &nuum, &b);

    if (a==num) printf("%d ", a);
    else {
        for(a; a!=num; a+=b) printf("%d ", a);
        printf("%d", num);
    }

    return 0;
}
